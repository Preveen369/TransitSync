<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TransitSync" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/blue</item>
        <item name="colorSecondary">@color/dark_blue</item>
        <item name="android:statusBarColor" targetApi="i">?attr/colorSecondary</item>
    </style>

    <style name="Theme.TransitSync" parent="Base.Theme.TransitSync" />
</resources>